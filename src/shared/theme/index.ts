import colors from "./colors";
import fontSize from "./font-size";
import fonts from "./fonts";
import theme from "./theme";

export { colors, fonts, fontSize, theme };
export default { colors, fonts, fontSize, theme };
